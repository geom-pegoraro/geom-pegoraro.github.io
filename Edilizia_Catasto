<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Geom. Pegoraro Alex — Pratiche Edilizie & Catastali</title>
<meta name="description" content="Contatta il Geom. Alex Pegoraro per pratiche edilizie, variazioni catastali, frazionamenti e accatastamenti. Colloquio preliminare disponibile.">
<meta name="robots" content="index, follow">
<link rel="canonical" href="https://geom-pegoraro.github.io/privati.html">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,300;0,400;0,500;0,600;1,300&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg:     #0f1117;
    --bg2:    #161b26;
    --bg3:    #1c2233;
    --border: rgba(255,255,255,0.07);
    --border2:rgba(255,255,255,0.13);
    --text:   #dde4f0;
    --muted:  #6e7d96;
    --dim:    #3d4a5e;
    --font:   'Inter', sans-serif;
    --mono:   'DM Mono', monospace;
    --r:      3px;
    --c-privati:   #1a8a5a;
    --c-privati-s: rgba(26,138,90,0.13);
    --c-privati-b: rgba(26,138,90,0.45);
    --c-wa:        #25d366;
    --c-wa-s:      rgba(37,211,102,0.12);
    --c-wa-b:      rgba(37,211,102,0.40);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font);
    font-weight: 300;
    font-size: 15px;
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  #bg-canvas {
    position: fixed; inset: 0;
    width: 100%; height: 100%;
    pointer-events: none;
    z-index: 0;
  }

  header {
    position: relative; z-index: 1;
    padding: 17px 0;
  }
  .hdr {
    max-width: 900px; margin: 0 auto; padding: 0 22px;
    display: flex; justify-content: space-between; align-items: center;
  }
  .hdr-name { font-size: 0.9rem; font-weight: 600; color: #fff; letter-spacing: -0.01em; }
  .hdr-sub { font-weight: 400; font-family: var(--mono); font-size: 0.62rem; color: var(--muted); letter-spacing: 0.08em; text-transform: uppercase; margin-top: 2px; }
  .hdr-back {
    font-family: var(--mono); font-size: 0.65rem; font-weight: 500;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--muted); text-decoration: none;
    border: 1px solid var(--border2); padding: 5px 13px; border-radius: 2px;
    transition: color 0.15s, border-color 0.15s;
    display: flex; align-items: center; gap: 7px;
  }
  .hdr-back:hover { color: #fff; border-color: rgba(255,255,255,0.3); }

  .main {
    position: relative; z-index: 1;
    flex: 1;
    display: flex;
    align-items: center;
    padding: 60px 0 50px;
  }
  .main-inner { max-width: 900px; margin: 0 auto; padding: 0 22px; width: 100%; }

  .page-eye {
    font-weight: 400; font-family: var(--mono); font-size: 0.67rem;
    letter-spacing: 0.14em; text-transform: uppercase; color: var(--muted);
    margin-bottom: 16px; display: flex; align-items: center; gap: 10px;
    animation: up 0.45s ease both 0.05s;
  }
  .page-eye::before { content: ''; display: block; width: 26px; height: 1px; background: var(--dim); flex-shrink: 0; }

  .page-title {
    font-size: clamp(1.8rem, 4vw, 2.8rem); font-weight: 600; line-height: 1.1;
    letter-spacing: -0.03em; color: #fff; margin-bottom: 14px;
    animation: up 0.45s ease both 0.13s;
  }

  .page-desc {
    font-size: 0.92rem; color: var(--muted); max-width: 560px;
    line-height: 1.75; margin-bottom: 48px;
    animation: up 0.45s ease both 0.21s;
  }

  .servizi-list {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    margin-bottom: 48px;
    animation: up 0.45s ease both 0.27s;
  }

  .srv-item {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-left: 3px solid var(--c-privati);
    border-radius: var(--r);
    padding: 18px 16px;
  }
  .srv-item-title {
    font-size: 0.82rem; font-weight: 600; color: #fff;
    letter-spacing: -0.01em; margin-bottom: 5px;
  }
  .srv-item-desc {
    font-size: 0.75rem; color: var(--muted); line-height: 1.6;
  }

  .contact-section {
    animation: up 0.45s ease both 0.33s;
  }
  .section-label {
    font-family: var(--mono); font-size: 0.6rem; font-weight: 500;
    letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--dim); margin-bottom: 14px;
    display: flex; align-items: center; gap: 10px;
  }
  .section-label::after { content: ''; flex: 1; height: 1px; background: var(--border); }

  .contact-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  .contact-card {
    display: flex; flex-direction: column;
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: var(--r);
    padding: 28px 24px;
    text-decoration: none;
    transition: background 0.18s, border-color 0.18s, transform 0.18s, box-shadow 0.18s;
    position: relative; overflow: hidden;
  }
  .contact-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    transform: scaleX(0); transform-origin: left;
    transition: transform 0.22s ease;
  }
  .contact-card:hover::before { transform: scaleX(1); }
  .contact-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 32px rgba(0,0,0,0.35);
  }

  .contact-card.email { border-left: 3px solid var(--c-privati); }
  .contact-card.email::before { background: var(--c-privati); }
  .contact-card.email:hover { background: var(--c-privati-s); border-color: var(--c-privati-b); }

  .contact-card.whatsapp { border-left: 3px solid var(--c-wa); }
  .contact-card.whatsapp::before { background: var(--c-wa); }
  .contact-card.whatsapp:hover { background: var(--c-wa-s); border-color: var(--c-wa-b); }

  .contact-icon {
    width: 44px; height: 44px;
    margin-bottom: 16px;
    flex-shrink: 0;
  }
  .contact-icon svg {
    width: 44px; height: 44px;
    display: block;
  }

  .contact-type {
    font-family: var(--mono); font-size: 0.6rem; font-weight: 500;
    letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--muted); margin-bottom: 6px;
  }
  .contact-title {
    font-size: 1.05rem; font-weight: 600; color: #fff;
    letter-spacing: -0.02em; margin-bottom: 6px;
  }
  .contact-value {
    font-family: var(--mono); font-size: 0.78rem; color: var(--muted);
    margin-bottom: 18px; flex: 1;
  }
  .contact-btn {
    display: inline-flex; align-items: center; gap: 8px;
    font-family: var(--mono); font-size: 0.68rem; font-weight: 500;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: #fff; padding: 8px 18px; border-radius: 2px;
    align-self: flex-start;
    transition: opacity 0.15s, gap 0.18s;
  }
  .contact-card.email   .contact-btn { background: var(--c-privati); }
  .contact-card.whatsapp .contact-btn { background: var(--c-wa); }
  .contact-card:hover .contact-btn { opacity: 0.88; gap: 13px; }

  footer {
    position: relative; z-index: 1;
    padding: 28px 0; margin-top: auto;
  }
  .ft {
    max-width: 900px; margin: 0 auto; padding: 0 22px;
    display: grid; grid-template-columns: 1fr auto;
    gap: 20px; align-items: center;
    border-top: 1px solid var(--border);
    padding-top: 24px;
  }
  .ft-info p { font-weight: 400; font-family: var(--mono); font-size: 0.75rem; color: var(--muted); line-height: 2.1; }
  .ft-info a { color: var(--text); text-decoration: none; }
  .ft-info a:hover { color: #fff; text-decoration: underline; }
  .ft-name { font-size: 0.95rem; font-weight: 600; color: #fff; margin-bottom: 6px; }
  .ft-right { font-weight: 400; font-family: var(--mono); font-size: 0.72rem; color: var(--muted); text-align: right; line-height: 2; }
  .ft-legal-inline { font-size: 0.60rem; color: var(--dim); margin-top: 4px; line-height: 1.5; }

  @media (max-width: 680px) {
    .servizi-list { grid-template-columns: 1fr; }
    .contact-cards { grid-template-columns: 1fr; }
    .ft { grid-template-columns: 1fr; }
    .ft-right { text-align: left; }
    .main { padding: 40px 0 36px; }
  }

  @keyframes up {
    from { opacity: 0; transform: translateY(14px); }
    to   { opacity: 1; transform: translateY(0); }
  }
</style>
<link rel="icon" type="image/png" href="favicon.png">
<link rel="apple-touch-icon" href="favicon.png">
</head>
<body>

<canvas id="bg-canvas"></canvas>

<header>
  <div class="hdr">
    <div>
      <div class="hdr-name">Geom. Pegoraro Alex</div>
      <div class="hdr-sub">Servizi Tecnici Grafici</div>
    </div>
    <a href="index.html" class="hdr-back">← Torna alla home</a>
  </div>
</header>

<div class="main">
  <div class="main-inner">

    <div class="page-eye">Privati — Pratiche Edilizie & Catastali</div>
    <h1 class="page-title">Come posso aiutarti?</h1>
    <p class="page-desc">
      Sei un privato e hai bisogno di un geometra? Mi occupo di pratiche edilizie e catastali per conto di privati cittadini. Contattami per un colloquio preliminare gratuito: valutiamo insieme la tua situazione e il percorso migliore da seguire.
    </p>

    <div class="servizi-list">
      <div class="srv-item">
        <div class="srv-item-title">CILA / SCIA / PDC</div>
        <div class="srv-item-desc">Comunicazioni e permessi per lavori di ristrutturazione, ampliamento o nuova costruzione.</div>
      </div>
      <div class="srv-item">
        <div class="srv-item-title">Variazioni Catastali</div>
        <div class="srv-item-desc">Aggiornamento della planimetria catastale dopo lavori o cambio di destinazione d'uso.</div>
      </div>
      <div class="srv-item">
        <div class="srv-item-title">Frazionamenti</div>
        <div class="srv-item-desc">Suddivisione di terreni o unità immobiliari in più parti distinte al catasto.</div>
      </div>
      <div class="srv-item">
        <div class="srv-item-title">Accatastamenti</div>
        <div class="srv-item-desc">Prima iscrizione al catasto di fabbricati nuovi o mai censiti.</div>
      </div>
      <div class="srv-item">
        <div class="srv-item-title">Colloquio Preliminare</div>
        <div class="srv-item-desc">Non sai da dove iniziare? Contattami per capire insieme cosa ti serve e come procedere.</div>
      </div>
      <div class="srv-item">
        <div class="srv-item-title">Altre Pratiche</div>
        <div class="srv-item-desc">Visure catastali, conformità urbanistica, certificazioni e consulenze tecniche varie.</div>
      </div>
    </div>

    <div class="contact-section">
      <div class="section-label">Contattami</div>
      <div class="contact-cards">

        <a href="mailto:pegoraroalexgeom@gmail.com" class="contact-card email">
          <div class="contact-icon">
            <svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg">
              <defs>
                <linearGradient id="mailBg" x1="0%" y1="0%" x2="0%" y2="100%">
                  <stop offset="0%" stop-color="#5AC8FA"/>
                  <stop offset="100%" stop-color="#2C7BE5"/>
                </linearGradient>
              </defs>
              <rect width="44" height="44" rx="10" fill="url(#mailBg)"/>
              <rect x="6" y="13" width="32" height="20" rx="2.5" fill="white"/>
              <polyline points="6,13 22,25 38,13" fill="none" stroke="#4ab0f0" stroke-width="2.2" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="contact-type">Email</div>
          <div class="contact-title">Scrivi una mail</div>
          <div class="contact-value">pegoraroalexgeom@gmail.com</div>
          <div class="contact-btn">Invia email →</div>
        </a>

        <a href="https://wa.me/393246303270?text=Salve%20Geom.%20Pegoraro%2C%20la%20contatto%20dal%20suo%20sito%20per%20informazioni%20su%20una%20pratica." target="_blank" class="contact-card whatsapp">
          <div class="contact-icon">
            <svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg">
              <rect width="44" height="44" rx="10" fill="#25D366"/>
              <path fill="white" d="M22 7.5C14.0 7.5 7.5 14.0 7.5 22C7.5 24.7 8.3 27.2 9.6 29.4L7.5 36.5L14.8 34.4C16.9 35.6 19.4 36.3 22 36.3C30.0 36.3 36.5 29.8 36.5 21.8C36.5 13.9 30.0 7.5 22 7.5ZM28.8 26.5C28.5 27.3 27.1 28.0 26.3 28.1C25.5 28.2 24.6 28.3 21.6 27.1C18.0 25.7 15.7 22.0 15.5 21.7C15.3 21.4 13.8 19.4 13.8 17.3C13.8 15.2 14.9 14.2 15.3 13.8C15.7 13.4 16.2 13.3 16.5 13.3C16.8 13.3 17.1 13.3 17.4 13.3C17.7 13.3 18.1 13.2 18.5 14.1C18.9 15.1 19.9 17.2 20.0 17.4C20.1 17.6 20.1 17.9 20.0 18.1C19.9 18.4 19.8 18.6 19.6 18.8C19.4 19.0 19.1 19.3 18.9 19.5C18.7 19.7 18.5 19.9 18.7 20.2C18.9 20.6 19.9 22.2 21.4 23.5C23.3 25.2 24.9 25.7 25.3 25.9C25.7 26.1 25.9 26.1 26.1 25.8C26.3 25.6 27.3 24.4 27.5 24.0C27.7 23.6 28.0 23.7 28.3 23.8C28.6 23.9 30.7 24.9 31.1 25.1C31.5 25.3 31.7 25.4 31.8 25.5C31.9 25.8 31.9 26.4 31.6 27.1Z"/>
            </svg>
          </div>
          <div class="contact-type">WhatsApp</div>
          <div class="contact-title">Scrivimi su WhatsApp</div>
          <div class="contact-value">+39 324 630 3270</div>
          <div class="contact-btn">Apri WhatsApp →</div>
        </a>

      </div>
    </div>

  </div>
</div>

<footer>
  <div class="ft">
    <div class="ft-info">
      <div class="ft-name">Geom. Pegoraro Alex</div>
      <p>Email: <a href="mailto:pegoraroalexgeom@gmail.com">pegoraroalexgeom@gmail.com</a></p>
      <p>P.IVA: 04505080244</p>
    </div>
    <div class="ft-right">
      <div>© 2025 Geom. Pegoraro Alex</div>
      <div>Servizi Tecnici Grafici</div>
      <div class="ft-legal-inline">Servizi di esclusivo supporto grafico tecnico alla progettazione.</div>
    </div>
  </div>
</footer>

<script>
(function() {
  const canvas = document.getElementById('bg-canvas');
  const ctx = canvas.getContext('2d');
  const PALETTE = ["255,255,255","200,220,255","180,200,255","220,230,255"];
  let W, H, nodes, hubs, t = 0;

  function resize() {
    W = canvas.width = window.innerWidth;
    H = canvas.height = window.innerHeight;
    build();
  }
  function rand(a, b) { return a + Math.random() * (b - a); }
  function pick() { return PALETTE[Math.floor(Math.random() * PALETTE.length)]; }

  function build() {
    nodes = [];
    const layers = [
      { count: 18, speed: 0.12, rMin: 0.5, rMax: 1.2, alpha: 0.35 },
      { count: 28, speed: 0.26, rMin: 0.8, rMax: 2.0, alpha: 0.50 },
      { count: 14, speed: 0.48, rMin: 1.2, rMax: 3.2, alpha: 0.65 },
    ];
    for (const l of layers) {
      for (let i = 0; i < l.count; i++) {
        const baseR = rand(l.rMin, l.rMax);
        nodes.push({
          x: rand(0, W), y: rand(0, H),
          vx: (Math.random()-0.5)*l.speed, vy: (Math.random()-0.5)*l.speed,
          r: baseR, baseR,
          pulseSpeed: rand(0.008, 0.022), pulsePhase: rand(0, Math.PI*2),
          alpha: l.alpha, col: pick(), layer: l.speed,
        });
      }
    }
    hubs = [];
    for (let i = 0; i < 5; i++) {
      hubs.push({
        x: rand(W*0.1, W*0.9), y: rand(H*0.1, H*0.9),
        vx: (Math.random()-0.5)*0.08, vy: (Math.random()-0.5)*0.08,
        r: rand(3.5, 6), ringPhase: rand(0, Math.PI*2), col: pick(),
      });
    }
  }

  function drawGradientLine(x1, y1, x2, y2, col1, col2, alpha) {
    const grad = ctx.createLinearGradient(x1, y1, x2, y2);
    grad.addColorStop(0, 'rgba('+col1+','+alpha+')');
    grad.addColorStop(1, 'rgba('+col2+','+alpha+')');
    ctx.beginPath(); ctx.strokeStyle = grad;
    ctx.moveTo(x1, y1); ctx.lineTo(x2, y2); ctx.stroke();
  }

  function draw() {
    ctx.clearRect(0, 0, W, H);
    const vg = ctx.createRadialGradient(W*0.28, H*0.38, 0, W*0.28, H*0.38, W*0.45);
    vg.addColorStop(0, 'rgba(15,17,23,0.82)');
    vg.addColorStop(0.5, 'rgba(15,17,23,0.45)');
    vg.addColorStop(1, 'rgba(15,17,23,0)');
    ctx.fillStyle = vg; ctx.fillRect(0, 0, W, H);
    t += 0.012;

    const MAX_DIST = 150;
    for (let i = 0; i < nodes.length; i++) {
      for (let j = i+1; j < nodes.length; j++) {
        if (Math.abs(nodes[i].layer - nodes[j].layer) > 0.01) continue;
        const dx = nodes[i].x - nodes[j].x, dy = nodes[i].y - nodes[j].y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < MAX_DIST) {
          const a = (1 - dist/MAX_DIST) * nodes[i].alpha * 0.6;
          ctx.lineWidth = 1.2;
          drawGradientLine(nodes[i].x, nodes[i].y, nodes[j].x, nodes[j].y, nodes[i].col, nodes[j].col, a);
        }
      }
    }
    const HUB_DIST = 220;
    for (const h of hubs) {
      for (const n of nodes) {
        const dx = h.x - n.x, dy = h.y - n.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < HUB_DIST) {
          ctx.lineWidth = 1.0;
          drawGradientLine(h.x, h.y, n.x, n.y, h.col, n.col, (1-dist/HUB_DIST)*0.28);
        }
      }
    }
    for (const n of nodes) {
      n.r = n.baseR + Math.sin(t * n.pulseSpeed / 0.012 + n.pulsePhase) * n.baseR * 0.35;
      ctx.beginPath(); ctx.arc(n.x, n.y, n.r, 0, Math.PI*2);
      ctx.fillStyle = 'rgba('+n.col+','+n.alpha+')'; ctx.fill();
      if (n.baseR > 1.8) {
        ctx.beginPath(); ctx.arc(n.x, n.y, n.r*2.5, 0, Math.PI*2);
        const glow = ctx.createRadialGradient(n.x, n.y, n.r*0.5, n.x, n.y, n.r*2.5);
        glow.addColorStop(0, 'rgba('+n.col+','+(n.alpha*0.3)+')');
        glow.addColorStop(1, 'rgba('+n.col+',0)');
        ctx.fillStyle = glow; ctx.fill();
      }
      n.x += n.vx; n.y += n.vy;
      if (n.x < 0 || n.x > W) n.vx *= -1;
      if (n.y < 0 || n.y > H) n.vy *= -1;
    }
    for (const h of hubs) {
      h.ringPhase += 0.008;
      const ringR = h.r + 6 + Math.sin(h.ringPhase) * 4;
      const ringAlpha = 0.08 + Math.sin(h.ringPhase) * 0.05;
      ctx.beginPath(); ctx.arc(h.x, h.y, ringR, 0, Math.PI*2);
      ctx.strokeStyle = 'rgba('+h.col+','+ringAlpha+')'; ctx.lineWidth = 1.2; ctx.stroke();
      const ring2R = h.r + 14 + Math.sin(h.ringPhase+1) * 6;
      ctx.beginPath(); ctx.arc(h.x, h.y, ring2R, 0, Math.PI*2);
      ctx.strokeStyle = 'rgba('+h.col+','+(ringAlpha*0.5)+')'; ctx.lineWidth = 1.2; ctx.stroke();
      const grad = ctx.createRadialGradient(h.x, h.y, 0, h.x, h.y, h.r*4);
      grad.addColorStop(0, 'rgba('+h.col+',0.55)');
      grad.addColorStop(0.4, 'rgba('+h.col+',0.15)');
      grad.addColorStop(1, 'rgba('+h.col+',0)');
      ctx.beginPath(); ctx.arc(h.x, h.y, h.r*4, 0, Math.PI*2);
      ctx.fillStyle = grad; ctx.fill();
      ctx.beginPath(); ctx.arc(h.x, h.y, h.r, 0, Math.PI*2);
      ctx.fillStyle = 'rgba('+h.col+',0.75)'; ctx.fill();

      const MIN_HUB_DIST = 180;
      for (const other of hubs) {
        if (other === h) continue;
        const dx = h.x - other.x, dy = h.y - other.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < MIN_HUB_DIST && dist > 0) {
          const force = (MIN_HUB_DIST - dist) / MIN_HUB_DIST * 0.04;
          h.vx += (dx / dist) * force;
          h.vy += (dy / dist) * force;
        }
      }
      const maxV = 0.5;
      const speed = Math.sqrt(h.vx*h.vx + h.vy*h.vy);
      if (speed > maxV) { h.vx = h.vx/speed*maxV; h.vy = h.vy/speed*maxV; }

      h.x += h.vx; h.y += h.vy;
      if (h.x < 0 || h.x > W) h.vx *= -1;
      if (h.y < 0 || h.y > H) h.vy *= -1;
    }

    requestAnimationFrame(draw);
  }

  window.addEventListener('resize', resize);
  resize(); draw();
})();
</script>

</body>
</html>
